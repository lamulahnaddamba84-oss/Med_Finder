# Laravel Website Setup Progress

## TODO Steps:

- [x]   1. Run `composer install` to install dependencies and generate vendor/autoload.php (complete, v12.56.0)
- [x]   2. Copy .env.example to .env if missing
- [x]   3. Run `php artisan key:generate`
- [x]   4. Verify fix: `php artisan --version`
- [x]   5. Install JS deps and build assets: `npm install && npm run build` (attempted; verify public/build/ dir)
- [x]   6. Setup database (if needed): `php artisan migrate` (graceful run)
- [x]   7. Start server: `php artisan serve` (http://127.0.0.1:8000)

## Next: Run step 5 via terminal commands below.
